import styled from "styled-components";

const HeroContainer = styled.div`
    height: 100vh;
    width: 100%;

`;

export default function Hero() {
  return (
    <HeroContainer>
      hero
    </HeroContainer>
  )
}
